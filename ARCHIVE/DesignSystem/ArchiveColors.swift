import SwiftUI

enum ArchiveColors {
    static let background = Color(nsColor: .windowBackgroundColor)
    static let surface = Color.white
    static let text = Color.black
    static let secondaryText = Color.black.opacity(0.48)
    static let inactiveText = Color.black.opacity(0.18)
    static let hairline = Color.black.opacity(0.10)
    static let field = Color.black.opacity(0.035)
    static let error = Color.black.opacity(0.72)
}
